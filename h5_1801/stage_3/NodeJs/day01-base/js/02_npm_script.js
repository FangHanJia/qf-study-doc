 
/**
 * npm脚本
 *     执行package.json中的scripts的代码
 *         1、属性名('FunA') ：路劲
 *             --执行：npm run 属性名
 *         2、简写单词(start,stop.test) ：属性名(npm run +属性名);
 *             --执行：npm 简写单词（start。。。）;
 */